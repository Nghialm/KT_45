/*
insert license info here
*/
using System;
using NHibernate;
using NHibernate.Cfg;
using Nullables;
using System.Collections;
using System.ComponentModel;
using System.Data;
using System.Collections.Generic;
using Vns.Erp.Core.Dao.NHibernate;
using Vns.Erp.Core.Admin.Domain;
using Vns.Erp.Core.Admin.Dao;
using Vns.Erp.Core.Dao;
using Spring.Transaction.Interceptor;
namespace Vns.Erp.Core.Admin.Dao.NHibernate
{
	/// <summary>
	///	Generated by MyGeneration using the NHibernate Object Mapping adapted by Gustavo
	/// </summary>
	[Serializable]
	public sealed class DmHanghoaDao:GenericDao<DmHanghoa,Guid>,IDmHanghoaDao
	{
        #region IDao
        DmHanghoa IDao<DmHanghoa, Guid>.Merge(DmHanghoa entity)
        {
            if (VnsCheck.IsNullGuid(entity.Id)) entity.Id = Guid.NewGuid();
            HibernateTemplate.Merge(entity);

            return entity;
        }

        DmHanghoa IDao<DmHanghoa, Guid>.Save(DmHanghoa entity)
        {
            if (VnsCheck.IsNullGuid(entity.Id)) entity.Id = Guid.NewGuid();
            HibernateTemplate.Save(entity);

            return entity;
        }

        DmHanghoa IDao<DmHanghoa, Guid>.SaveOrUpdate(DmHanghoa entity)
        {
            if (VnsCheck.IsNullGuid(entity.Id)) entity.Id = Guid.NewGuid();
            HibernateTemplate.SaveOrUpdate(entity);

            return entity;
        }
        #endregion

        public int GetNumberHangHoaByKyHieu(String KyHieu, Guid DonviId)
        {
            string sql = "select count(*) from DmHanghoa a where a.DonviId= :DonviId and a.KyHieu like :KyHieu";
            IQuery q = NHibernateSession.CreateQuery(sql);
            q.SetParameter("DonviId", DonviId);
            q.SetParameter("KyHieu", KyHieu);
            try
            {
                Int32 i = Convert.ToInt32(q.UniqueResult());
                return i;
            }
            catch
            {
                return 1;
            }
        }
        public IList<DmHanghoa> GetAllByDonviID(Guid DonviId)
        {
            ArrayList props = new ArrayList();
            ArrayList values = new ArrayList();
            props.Add("DonviId");
            values.Add(DonviId);
            return List(-1, -1, props, values, null);
        }
        public void DeleteAllByDonviId(Guid DonviId)
        {
            string sql = "Delete from DmHanghoa a where a.DonviId= :DonviId ";
            IQuery q = NHibernateSession.CreateQuery(sql);
            q.SetParameter("DonviId", DonviId);

            q.ExecuteUpdate();
        }
	}
}
