/*
insert license info here
*/
using System;
using NHibernate;
using NHibernate.Cfg;
using Nullables;
using System.Collections;
using System.ComponentModel;
using System.Data;
using System.Collections.Generic;
using Vns.Erp.Core.Dao.NHibernate;
using Vns.Erp.Core.Admin.Domain;
using Vns.Erp.Core.Admin.Dao;
using Vns.Erp.Core.Dao;
using Spring.Transaction.Interceptor;
namespace Vns.Erp.Core.Admin.Dao.NHibernate
{
	/// <summary>
	///	Generated by MyGeneration using the NHibernate Object Mapping adapted by Gustavo
	/// </summary>
	[Serializable]
	public sealed class DmNhomvattu1Dao:GenericDao<DmNhomvattu1,Guid>,IDmNhomvattu1Dao
	{
        #region IDao
        DmNhomvattu1 IDao<DmNhomvattu1, Guid>.Merge(DmNhomvattu1 entity)
        {
            if (VnsCheck.IsNullGuid(entity.Id)) entity.Id = Guid.NewGuid();
            HibernateTemplate.Merge(entity);

            return entity;
        }

        DmNhomvattu1 IDao<DmNhomvattu1, Guid>.Save(DmNhomvattu1 entity)
        {
            if (VnsCheck.IsNullGuid(entity.Id)) entity.Id = Guid.NewGuid();
            HibernateTemplate.Save(entity);

            return entity;
        }

        DmNhomvattu1 IDao<DmNhomvattu1, Guid>.SaveOrUpdate(DmNhomvattu1 entity)
        {
            if (VnsCheck.IsNullGuid(entity.Id)) entity.Id = Guid.NewGuid();
            HibernateTemplate.SaveOrUpdate(entity);

            return entity;
        }
        #endregion

        public IList<DmNhomvattu1> GetObjectAllByDonviID(Guid DonViId)
        {
            ArrayList props = new ArrayList();
            ArrayList values = new ArrayList();
            props.Add("DonviId");
            values.Add(DonViId);
            return List(-1, -1, props, values, null);
        }


        public IList<DmNhomvattu1> GetByMaNhomVT(string MaNhom, Guid DonViId)
        {
            string sql = "from DmNhomvattu1 a where a.MaNhomVt1 like (:MaNhom) and a.DonviId = (:DonviId)";
            IQuery q = NHibernateSession.CreateQuery(sql);
            q.SetParameter("MaNhom", MaNhom);
            q.SetParameter("DonviId", DonViId);
            return q.List<DmNhomvattu1>();
        }
        public IList<DmNhomvattu1> GetAllByNhom(Guid DonViId, Decimal LoaiNhom)
        {
            string sql = "from DmNhomvattu1 a where a.LoaiNhom = :LoaiNhom and a.DonviId = :DonviId";
            IQuery q = NHibernateSession.CreateQuery(sql);
            q.SetParameter("LoaiNhom", LoaiNhom);
            q.SetParameter("DonviId", DonViId);
            return q.List<DmNhomvattu1>();
        }        
    }
}
