/*
insert license info here
*/
using System;
using NHibernate;
using NHibernate.Cfg;
using Nullables;
using System.Collections;
using System.ComponentModel;
using System.Data;
using System.Collections.Generic;
using Vns.Erp.Core.Dao.NHibernate;
using Vns.Erp.Core.Admin.Domain;
using Vns.Erp.Core.Admin.Dao;
using Vns.Erp.Core.Dao;
using Spring.Transaction.Interceptor;
namespace Vns.Erp.Core.Admin.Dao.NHibernate
{
	/// <summary>
	///	Generated by MyGeneration using the NHibernate Object Mapping adapted by Gustavo
	/// </summary>
	[Serializable]
	public sealed class DmHopdongDao:GenericDao<DmHopdong,Guid>,IDmHopdongDao
	{
        #region IDao
        DmHopdong IDao<DmHopdong, Guid>.Merge(DmHopdong entity)
        {
            if (VnsCheck.IsNullGuid(entity.Id)) entity.Id = Guid.NewGuid();
            HibernateTemplate.Merge(entity);

            return entity;
        }

        DmHopdong IDao<DmHopdong, Guid>.Save(DmHopdong entity)
        {
            if (VnsCheck.IsNullGuid(entity.Id)) entity.Id = Guid.NewGuid();
            HibernateTemplate.Save(entity);

            return entity;
        }

        DmHopdong IDao<DmHopdong, Guid>.SaveOrUpdate(DmHopdong entity)
        {
            if (VnsCheck.IsNullGuid(entity.Id)) entity.Id = Guid.NewGuid();
            HibernateTemplate.SaveOrUpdate(entity);

            return entity;
        }
        #endregion

        public IList<DmHopdong> GetAllByDonviID(Guid DonviId)
        {
            string sql = "from DmHopdong a where a.DonviId = :DonviId";
            IQuery q = NHibernateSession.CreateQuery(sql);
            q.SetParameter("DonviId", DonviId);
            return q.List<DmHopdong>();
        }
        public IList<DmHopdong> GetAllByKy_Hieu(string KyHieu, Guid DonviId)
        {
            string sql = "from DmHopdong a where a.DonviId = :DonviId and a.KyHieu like (:KyHieu)";
            IQuery q = NHibernateSession.CreateQuery(sql);
            q.SetParameter("DonviId", DonviId);
            q.SetParameter("KyHieu", KyHieu);            
            return q.List<DmHopdong>();
        }
	}
}
