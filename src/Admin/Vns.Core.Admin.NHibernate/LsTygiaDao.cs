/*
insert license info here
*/
using System;
using NHibernate;
using NHibernate.Cfg;
using Nullables;
using System.Collections;
using System.ComponentModel;
using System.Data;
using System.Collections.Generic;
using Vns.Erp.Core.Dao.NHibernate;
using Vns.Erp.Core.Admin.Domain;
using Vns.Erp.Core.Admin.Dao;
using Vns.Erp.Core.Dao;
using Spring.Transaction.Interceptor;
namespace Vns.Erp.Core.Admin.Dao.NHibernate
{
	/// <summary>
	///	Generated by MyGeneration using the NHibernate Object Mapping adapted by Gustavo
	/// </summary>
	[Serializable]
	public sealed class LsTygiaDao:GenericDao<LsTygia,Guid>,ILsTygiaDao
	{
        #region IDao
        LsTygia IDao<LsTygia, Guid>.Merge(LsTygia entity)
        {
            if (VnsCheck.IsNullGuid(entity.Id)) entity.Id = Guid.NewGuid();
            HibernateTemplate.Merge(entity);

            return entity;
        }

        LsTygia IDao<LsTygia, Guid>.Save(LsTygia entity)
        {
            if (VnsCheck.IsNullGuid(entity.Id)) entity.Id = Guid.NewGuid();
            HibernateTemplate.Save(entity);

            return entity;
        }

        LsTygia IDao<LsTygia, Guid>.SaveOrUpdate(LsTygia entity)
        {
            if (VnsCheck.IsNullGuid(entity.Id)) entity.Id = Guid.NewGuid();
            HibernateTemplate.SaveOrUpdate(entity);

            return entity;
        }
        #endregion

        public IList<LsTygia> GetObjectByDoiTuong(string TenNgoaiteHt)
        {
            string sql = "from LsTygia a where a.TenNgoaiteHt like (:TenNgoaiteHt)";
            IQuery q = NHibernateSession.CreateQuery(sql);
            q.SetParameter("TenNgoaiteHt", TenNgoaiteHt);
            return q.List<LsTygia>();
        }
        public IList<LsTygia> GetTyGiaByNgoaiTeId(Guid NgoaiteId)
        {
            string sql = "from LsTygia a where a.NgoaiteId = (:NgoaiteId)";
            IQuery q = NHibernateSession.CreateQuery(sql);
            q.SetParameter("NgoaiteId", NgoaiteId);
            return q.List<LsTygia>();
        }
        public LsTygia GetTyGiaNgoaiTe(Guid NgoaiteId, DateTime NgayTyGia)
        {
            string sql = "from LsTygia ls where ls.NgoaiteId = :NgoaiteId and ls.NgayQuydoi <= :NgayQuydoi " +
                         " Order by ls.NgayQuydoi desc";
            IQuery q = NHibernateSession.CreateQuery(sql);
            q.SetMaxResults(1);
            q.SetParameter("NgoaiteId", NgoaiteId);
            q.SetParameter("NgayQuydoi", NgayTyGia.Date);
            IList<LsTygia> ls = q.List<LsTygia>();
            if (ls == null || ls.Count == 0)
                return null;
            else
                return ls[0];
        }
	}
}
